image: debian/stable
packages:
  - meson
  - libgoogle-glog-dev
  - nlohmann-json3-dev
  - libboost-all-dev
  - pkg-config
sources:
  - git@git.sr.ht:~ragreener1/contagent
tasks:
  - setup: |
      cd contagent
      meson setup builddir
  - compile: |
      cd contagent
      ninja -C builddir
